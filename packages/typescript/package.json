{
  "name": "chat-context-index",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "cci: persistent, retrievable conversation memory for AI applications (TypeScript native parity)",
  "license": "Apache-2.0",
  "engines": { "node": ">=22" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "LICENSE", "README.md", "UPSTREAM.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "13.0.3"
  },
  "optionalDependencies": {
    "redis": "^5.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
